// DemoDataEngine REMOVED — This component has been decommissioned.
// The DukaPos platform is running in PRODUCTION mode.
// Demo data seeding, reset commands, rollbacks, and sandbox provisioning
// are not permitted in the production environment.

import React from 'react';

export const DemoDataEngine: React.FC = () => null;
